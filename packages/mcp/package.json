{
  "name": "@omniui/mcp",
  "version": "0.0.0",
  "description": "Model Context Protocol server for omniUI. Lets coding agents list, search, install, and lint components conversationally.",
  "license": "MIT",
  "bin": {
    "omniui-mcp": "src/bin.ts"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "echo 'todo: wire eslint'",
    "build": "echo 'todo: wire tsup for cli bundle'",
    "start": "echo 'todo: run dist/bin.js'",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@omniui/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
